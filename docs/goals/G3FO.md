# Goal G3FO: Failure-first externalized cognition

- Status: active under a distinct no-budget Goal
- Anchor: protected `main` at `ed6f5922815a29b856786660e76d62b68eeae26a`
- Authority: RCI v0.5, PLAN, ADR-0012, ADR-0013, and ADR-0016
- Token budget: none

## Current and desired relation

Current: operational returns, predictions, mismatches, development evidence, and
manually authored project limitations are preserved, but no deterministic bridge
establishes which protected task consequence failed or which discriminator should be
asked next.

Desired: a pre-return evaluation protocol and the existing authoritative lifecycle
derive an exact evaluation result, an unresolved failure-localization frame, an inert
limitation candidate only for protected mismatch, and a bounded cognitive handoff.

## Separator

Under identical task pins, correct output, false necessity, may/must collapse, malformed
decode, timeout, and unsupported execution produce distinct lawful canonical outcomes.
The two semantic mismatches retain different protected-consequence identities, evidence,
attacks, and next questions. Operational and decode failures never become semantic
falsity or implementation blame.

## Preserve

- all sealed G1–G3G event schemas and replay semantics;
- request/attempt/return/decode/check/warrant/promotion separation;
- exact CAS evidence and lawful `Unknown`;
- every predecessor acceptance gate;
- no source-writing, arbitrary-command, Git, model, review, warrant, or promotion port.

## Required implementation

- strict frozen versioned protocol/result/localization/candidate/handoff records;
- pure permutation-stable evaluation over exact authoritative lifecycle records;
- CAS publication and verification of the protocol before evaluation;
- canonical SDK inspection and `rci project evaluate` / `rci project handoff`;
- deterministic weak-reasoner circuit/two-route fixture with exact cost accounting;
- no event or snapshot schema changes and no new dependency.

## Acceptance

```text
uv run pytest -q tests/acceptance/test_capability_failure_observation.py
```

Acceptance covers all six outcomes, mutation/staleness/foreign-evidence rejection,
late/unaccepted-return rejection, input-order stability, replay effect freedom,
context-reset handoff, failed-route reopening discipline, inert model prose, and bounded
fixture improvement without a universal model-equivalence or Pareto claim.

## Stop and follow-on

Stop after the derived boundary, SDK/CLI, deterministic fixture, and all predecessor
gates pass. Construct the next frontier from observed loci. Generalize dependent
question frames only when question/frame blindness is selected; require two distinct
measured failure bindings before a repository-wide relation package.
