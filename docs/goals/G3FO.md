# Goal G3FO: Failure-first externalized cognition

- Status: `stopped_indeterminate`
- Anchor: protected `main` at `ed6f5922815a29b856786660e76d62b68eeae26a`
- Candidate: `b3cb06363ac5b8cd60c9bfbda5c3fbdd7dca72fb`
- Pull request: #16, closed without merge; candidate branch retained
- Token budget: none

## Sealed relation

Current: operational returns, predictions, mismatches, development evidence, and
manually authored project limitations are preserved, but no deterministic bridge
establishes which protected task consequence failed or which discriminator should be
asked next.

Desired: a pre-return evaluation protocol and the existing authoritative lifecycle
derive an exact evaluation result, unresolved failure-localization frame, inert
limitation candidate only for protected mismatch, and bounded cognitive handoff.

Separator: under identical task pins, correct output, false necessity, may/must
collapse, malformed decode, timeout, and unsupported execution produce distinct lawful
canonical outcomes without collapsing effect, decode, check, semantic, or authority
stages.

## Preserved candidate evidence

The final candidate passed:

- focused G3FO: 21 tests;
- non-optional suite: 215 tests, one documented skip, four deselected;
- complete suite: 219 tests, one documented skip;
- acceptance suite: 56 tests;
- every sealed predecessor focused gate;
- Ruff format/check, mypy over 121 source files, import, CLI help, and build;
- every unique hosted exact-head CI job.

It also retained all sealed event/snapshot schemas and added no dependency, model
adapter, source/Git port, warrant, review substitution, or promotion authority.

## Stop return

Three earlier candidate revisions received fresh invalid semantic reviews and were
repaired. A fresh review of the final exact head was required before promotion, but the
hosted Codex reviewer reported exhausted review quota, GitHub Copilot returned no review,
and the local Codex executable was inaccessible. That is `Unknown`, not validation.

G3FO is therefore stopped indeterminate. No candidate code, focused CI requirement,
derived evaluator, handoff, or claimed weak-reasoner improvement entered protected
`main`.

## Reopening conditions

Reopening requires a new Goal from a fresh protected anchor plus one of:

- a fresh distinct-context exact-head semantic review capability;
- a surviving reproducible semantic breaker;
- a separately governed review-policy change;
- a narrower independently checkable slice that does not claim the blocked standing.

The candidate branch and PR returns remain evidence. They are not standing capability.
