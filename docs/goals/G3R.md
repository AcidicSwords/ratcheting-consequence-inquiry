# Sealed Goal G3R: Recursive project inquiry and candidate development

- Status: sealed and verified on protected `main` at
  `6b0f2f793130d91bddaf1a2cffa9be87fd8b4e6d`
- Authority: RCI v0.5, PLAN, ADR-0012, ADR-0013
- Token budget: none

## Target

Build and verify a repository-grounded recursive project-inquiry and isolated
candidate-development protocol. Preserve all sealed G1, G2A, G2B, and G3A-H event
schemas and meanings.

Implement strict records and new version-1 events for clean anchors, consequential
limitations, inert question/method candidates, repertoire decisions, successor
candidates, deterministic partial-order frontiers, immutable implementation Goals,
candidate environments, exact development evidence, fresh independent reviews,
project-successor decisions, externally observed promotion, cycle checkpoints, and
typed stopping. Fold into `InquiryState` schema v5 and rebuild v1-v4 snapshots.

Expose SDK methods and canonical-JSON `rci project` commands. The runtime must not gain a
source-writing, arbitrary-command, Git, merge, push, policy-editing, credential,
deployment, release, or authority-expansion port.

Dogfood the protocol against these alternatives: G3A-L exact linear binding,
regenerative question-contract synthesis, native-method binding, autonomous Goal
synthesis, isolated candidate-development actuation, and G4 recursive formal machinery.
Record the checked frontier and seal exactly one next no-budget Goal. Do not implement
that successor until G3R is verified and merged.

## Completion gate

Run every sealed predecessor gate and:

```text
uv run pytest -q tests/acceptance/test_recursive_project_inquiry.py
```

Completion requires RCI-001 through RCI-078 parity, unchanged archived stream semantics,
all six hosted checks including `recursive (ubuntu-latest)`, fresh independent review,
protected merge, and successful post-merge main CI.

All completion conditions passed on 2026-08-22. Exact local, review, pull-request,
protection, merge, and post-merge returns are recorded in `docs/verification.md`.

## Explicit exclusions

No SymPy/NumPy, G3A-L/G3B/G3C implementation, CHC/PDR, control, multi-backend warrant,
opaque end-to-end benchmark, unrestricted autonomous source authority, release, tag,
deployment, server, or UI.
